---
import Layout from "./Layout.astro";
import BaseSEO from "../components/utils/base/BaseSEO.astro";

interface Props {
  title: string;
  description: string;
  pageKey?: string;
}

const { title, description, pageKey } = Astro.props;
---

<Layout pageKey={pageKey}>
  <BaseSEO
    slot="seo"
    currentPage={pageKey}
    title={title}
    description={description}
    jsonType="Article"
    useTagLine={true}
  />

  <div class="w-full">
    <header class="bg-accent mx-auto w-full pt-12 pb-32">
      <div class="container">
        <h1
          id="legal-title"
          class="text-center text-2xl font-bold text-white! md:text-5xl"
        >
          {title}
        </h1>
        <p class="mt-2 mb-8 text-center text-lg text-white md:text-2xl">{description}</p>
      </div>
    </header>

    <article
      aria-labelledby="legal-title"
      class="prose prose-slate prose-headings:font-title prose-a:text-accent hover:prose-a:text-accent-dark max-h-auto -m-24 mx-auto mb-24 block w-full max-w-4xl rounded-2xl border border-white bg-white pb-12 shadow-xl"
    >
      <div class="content container mx-auto max-w-3xl">
        <slot />
      </div>
    </article>
  </div>
</Layout>
